const vscode = require('vscode');

class FormBehaviorCompletionProvider {
    /**
     * Form Behaviour completion provider.
     *
     * Provides autocompletion for certain keys in the form behaviour configuration.
     */
    constructor(modelScanner) {
        this.modelScanner = modelScanner;
    }


}
