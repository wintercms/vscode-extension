// const YAML = require('yaml');

class YamlTree {
    constructor(yaml) {
        this.yaml = yaml;
    }

    // isPath(...path) {

    // }

    hierarchy() {
        // const doc = YAML.parseDocument(this.yaml);
        // console.log(doc);
    }
}

module.exports = YamlTree;
