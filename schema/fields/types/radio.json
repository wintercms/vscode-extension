{
    "$schema": "http://json-schema.org/draft-07/schema",
    "definitions": {
        "value": {
            "title": "Radio field type",
            "description": "Renders a list of radio options, where only one item can be selected at a time",
            "type": "string",
            "const": "radio"
        },
        "properties": {
            "properties": {
                "options": {
                    "title": "Radio options",
                    "description": "Defines the available options for this radio list.",
                    "oneOf": [
                        {
                            "type": "string"
                        },
                        {
                            "type": "object"
                        },
                        {
                            "type": "array"
                        }
                    ]
                }
            }
        }
    }
}
